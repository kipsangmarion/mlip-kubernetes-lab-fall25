apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-load-balancer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-load-balancer
  template:
    metadata:
      labels:
        app: flask-load-balancer
    spec:
      containers:
      - name: flask-load-balancer
        # TODO: Replace <DockerHub-Username> with your Docker Hub username, and use the appropriate image name and tag
        image: <DockerHub-Username>/<load-balancer-image-name>:<load-balancer-image-tag>
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: flask-load-balancer-service 
spec:
  type: NodePort
  selector:
    app: flask-load-balancer
  ports:
    - name: http
      port: 8080          # service port inside cluster
      targetPort: 8080    # pod/container port
      nodePort: 30080   